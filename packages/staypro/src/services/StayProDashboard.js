document.addEventListener("DOMContentLoaded", () => {
    console.log("Dashboard StayPro Caricata!");
  
    // Funzione per navigare alle sezioni principali
    window.goTo = (section) => {
      alert(`Navigazione alla sezione ${section} (da implementare)!`);
    };
  });
  